# Клиент и модели, без сервера: этот модуль — потребитель публичного API, а не
# его реализация. Путь вывода задаётся флагом -o в scripts/generate.sh, чтобы
# та же конфигурация годилась и для генерации во временный каталог при --check.
package: tatnet
generate:
  models: true
  client: true
